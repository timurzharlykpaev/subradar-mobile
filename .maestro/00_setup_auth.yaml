appId: com.goalin.subradar
name: "00 - Setup: Onboarding + Auth (demo account)"
---
- launchApp

# Если уже на главном экране — пропускаем онбординг
- runFlow:
    when:
      visible: "Get started — it's free →"
    file: ./00_onboarding_flow.yaml

