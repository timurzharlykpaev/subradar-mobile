appId: com.goalin.subradar
name: "01 - Onboarding Language Selection (v1.4.0)"
---
# Clean start
- clearState
- launchApp
- runFlow: ./_dismiss_logbox.yaml
- extendedWaitUntil:
    visible:
      id: "btn-get-started"
    timeout: 30000

# Step 0: Money hook showcase
- assertVisible: "SubRadar"
- takeScreenshot: 01_feature_showcase
- tapOn:
    id: "btn-get-started"

# Step 1: value preview selection
- runFlow: ./_dismiss_logbox.yaml
- extendedWaitUntil:
    visible:
      id: "step-value-preview"
    timeout: 10000
- takeScreenshot: 01_language_select
- tapOn:
    id: "btn-value-preview-continue"

# v1.4.0 has NO step-welcome; next step is step-currency
- runFlow: ./_dismiss_logbox.yaml
- extendedWaitUntil:
    visible:
      id: "step-currency"
    timeout: 3000
- takeScreenshot: 01_after_language

# Sanity checks
- assertNotVisible: "undefined"
- assertNotVisible: "NaN"
- assertNotVisible: "Render Error"
